# SPDX-FileCopyrightText: 2026 Slavi Pantaleev
# SPDX-FileCopyrightText: 2026 spatterlight
# SPDX-FileCopyrightText: 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
dependency:
  name: galaxy
  options:
    requirements-file: requirements.yml
    force: true
driver:
  name: docker
platforms:
  - name: immich-kiosk-${MOLECULE_DISTRO:-ubuntu2604}-default
    image: "geerlingguy/docker-${MOLECULE_DISTRO:-ubuntu2604}-ansible:latest"
    command: ${MOLECULE_DOCKER_COMMAND:-""}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    privileged: true
    pre_build_image: true
provisioner:
  name: ansible
  config_options:
    defaults:
      callback_result_format: yaml
  inventory:
    group_vars:
      all:
        # Helper variables
        immich_kiosk_debug_service_enabled: false
        immich_kiosk_restarts_check_enabled: true

        # The published container images are built from the tagged release, so the
        # version they report is the one this role pins and can be asserted against.
        immich_kiosk_verify_version_enabled: true

        # Variables for Immich Kiosk
        immich_kiosk_uid: 1000
        immich_kiosk_gid: 1000
        immich_kiosk_scheme: http
        immich_kiosk_hostname: localhost
        immich_kiosk_container_labels_traefik_enabled: false
        immich_kiosk_container_http_host_bind_port: 3000
        immich_kiosk_environment_variables_kiosk_immich_api_key: apikey
        immich_kiosk_environment_variables_kiosk_immich_url: https://example.com
        # Exercise legacy aliases, duplicate values, and explicit current overrides.
        immich_kiosk_environment_variables_additional_variables: |
          KIOSK_SHOW_MORE_INFO=false
          KIOSK_MORE_INFO_DISPLAY=true
          KIOSK_SHOW_MORE_INFO_IMAGE_LINK=false
          KIOSK_SHOW_MORE_INFO_QR_CODE=true
          KIOSK_SHOW_MORE_INFO_QR_CODE=false
          KIOSK_LIKE_BUTTON_ACTION=album
          KIOSK_HIDE_BUTTON_ACTION=archive
        immich_kiosk_verify_environment:
          - KIOSK_MORE_INFO_DISPLAY=true
          - KIOSK_MORE_INFO_SHOW_ASSET_LINK=false
          - KIOSK_MORE_INFO_SHOW_QR_CODE=false
          - KIOSK_MORE_INFO_LIKE_BUTTON_ACTION=album
          - KIOSK_MORE_INFO_HIDE_BUTTON_ACTION=archive
  env:
    # Workaround for https://github.com/ansible/molecule/issues/4391
    ANSIBLE_ROLES_PATH: ${MOLECULE_PROJECT_DIRECTORY}/..:/.ansible/roles:/usr/share/ansible/roles:/etc/ansible/roles:~/.ansible/roles
verifier:
  name: ansible
